<!-- <% @movie_genres.each do |movie| %>
    <%= movie.genre %>
<% end %> -->

<%= form_with url: movies_path, method: :get do |form| %>
    <%= form.collection_radio_buttons :genre_name, @movie_genres, :genre, :genre %>
    <%= form.submit "Filter" %>
<% end %>

<% if !params[:genre_name] %>
    <h2>Newest Releases</h2>

    <% @newest_movies.each do |movie| %>
        <%= link_to "#{movie.title} (#{movie.year})", movie_path(movie) %>
        <br>
    <% end %>
<% else %>
    <% @movies.each do |movie| %>
        <%= link_to "#{movie.title} (#{movie.year})", movie_path(movie) %>
        <br>
    <% end %>
<% end %>