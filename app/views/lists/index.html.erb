<h1>Browse through all lists</h1>

<%= form_tag movies_path, method: :get do %>
    <%= search_field_tag :search, nil, placeholder: "Search for a list" %>
    <%= submit_tag "Search", name: nil %>
<% end %>
<br><br>
<% @lists.each do |list| %>
    <%= link_to list.title, list_path(list) %><br>
<% end %>