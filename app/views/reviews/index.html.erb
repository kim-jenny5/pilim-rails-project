<div class="container my-5 review_container">
  <% if @user %>
    <%= render "user_reviews" %>
  <% elsif @movie %>
    <!-- <h1>All Reviews for <%= link_to @movie.title, movie_path(@movie) %></h1> -->
    <%= render "movie_reviews" %>
  <% end %>
</div>
