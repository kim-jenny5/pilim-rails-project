<h2>@<%= @user.username %></h2>
<p>Joined: <%= @user.joined %></p>
<p><%= link_to "#{@user.username}'s Reviews", user_reviews_path(@user) %></p>
<h3><%= @user.username %>'s Lists</h3>
<% @user.lists.each do |list| %>
    <%= link_to list.title, list_path(list) %><br>
<% end %>
